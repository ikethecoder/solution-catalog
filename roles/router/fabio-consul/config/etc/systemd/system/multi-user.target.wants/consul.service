[Unit]
Description=Consul
After=network.target

[Service]
User=consul
Restart=always
ExecStart=/usr/local/bin/consul agent -bind {{PRIVATE_IPV4}} -bootstrap -server -data-dir /opt/consul/data -config-dir /etc/consul.d

[Install]
WantedBy=local.target