{
  "type": "exec",
  "attributes": {
    "run_if": [
      "passed"
    ],
    "command": "docker-run",
    "arguments": [ "canzea/canzea_cli", "mvn", "clean", "install" ],
    "working_directory": "{{project}}"
  }
}