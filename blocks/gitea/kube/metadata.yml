name: gitea
version: tbd
resources:
  - service_endpoint:
      ${instance.id}-gitea:
        service: gitea
        instance: ${instance.id}
        environment: ${environment.id}
        segment: ${segment.id}
        type: console
        secure: "https://source.cloud.${es.fqdn}"
