name: gocd
version: tbd
resources:
  - service_endpoint:
      ${instance.id}-gocd:
        service: gocd
        instance: ${instance.id}
        environment: ${environment.id}
        segment: ${segment.id}
        type: console
        secure: "https://gocd.cloud.${es.fqdn}"
